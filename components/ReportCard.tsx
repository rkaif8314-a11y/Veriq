"use client";import Link from "next/link";import type {Analysis} from "../lib/analyzer";
export default function ReportCard({result,id}:{result:Analysis;id?:string}){return <div className="result"><div className="result-main"><div><span className={"risk "+result.risk}>{result.risk.toUpperCase()}</span><h3>{result.headline}</h3><p>{result.summary}</p></div><div className="score"><strong>{result.score}</strong><span>/100</span><small>trust</small></div></div><div className="result-meta"><span>Confidence <b>{result.confidence}%</b></span><span>Evidence strength <b>{result.signals.length?"Mixed":"Insufficient"}</b></span></div><div className="signals">{result.signals.map((s,i)=><div className="signal" key={i}><i className={s.kind}></i><div><strong>{s.label}</strong><p>{s.detail}</p></div><span>{s.confidence}%</span></div>)}</div><div className="recommend"><b>What to do</b><p>{result.recommendation}</p></div><details><summary>Limitations & methodology</summary><ul>{result.limitations.map((x,i)=><li key={i}>{x}</li>)}</ul></details>{id&&<Link className="report-link" href={"/report/"+id}>Open full report →</Link>}</div>}