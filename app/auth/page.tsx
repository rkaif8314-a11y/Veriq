"use client";import Shell from "../../components/Shell";import {useState} from "react";import {createClient} from "../../lib/supabase-browser";
export default function Page(){const [email,setEmail]=useState("");const [status,setStatus]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const supabase=createClient();if(!supabase){setStatus("Authentication is not connected. Add NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY in Vercel to enable accounts.");return}const {error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:location.origin+"/dashboard"}});setStatus(error?.message||"Check your email for a secure sign-in link.")}return <Shell><main className="auth page shell"><div className="auth-card"><span className="kicker">VERIQ ACCOUNT</span><h1>Sign in securely.</h1><p>Use a passwordless email link. No password is stored by Veriq.</p><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><button className="button primary" type="submit">Send secure link →</button></form>{status&&<div className="notice">{status}</div>}</div></main></Shell>}