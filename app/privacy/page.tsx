import Shell from "../../components/Shell";export default function Page(){return <Shell><main className="page shell"><span className="kicker">PRIVACY</span><h1>Your data should not become the product.</h1><p className="lede">Veriq is designed around data minimization. The current MVP stores browser history locally for convenience and does not claim to use private content for model training.</p><div className="prose"><h2>What the MVP does</h2><p>Analysis requests are sent to the Veriq server endpoint so secrets and analysis logic remain off the client. URL fetching is server-side and blocks private-network destinations.</p><h2>What is not implemented yet</h2><p>Persistent account storage, uploaded-file retention controls, enterprise audit logging and third-party reputation providers require the next infrastructure phase.</p><h2>Your control</h2><p>You can clear local report history from Settings. When cloud storage is connected, deletion and export controls will be enforced through database policies.</p></div></main></Shell>}